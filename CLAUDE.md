# Leet9 — Claude Code Guidelines

## Language

All user-facing copy shipped in code must be in **English**:
- Email subjects, bodies, CTA labels, footers (`lib/email.js`)
- UI text in components and pages
- Error messages returned by API routes
- Any string that ends up visible to end users

Italian is only acceptable in chat messages between the developer and Claude, never in production code.
